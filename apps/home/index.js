document.addEventListener("DOMContentLoaded", function(event) {
    authCheck();

    //SATRT changing the dropdown menu bootstrap color
    const dropdownMenu = document.querySelector('.dropdown-menu');
    dropdownMenu.style.backgroundColor = '#00294c';
    //END changing the dropdown menu bootstrap color
});